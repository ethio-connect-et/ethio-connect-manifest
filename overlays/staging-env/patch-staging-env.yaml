# Patch for staging environment: use staging image tags and moderate resources
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vendor-portal-api
spec:
  template:
    spec:
      containers:
        - name: vendor-portal-api
          image: ghcr.io/ethioconnect/vendor-portal-api:staging
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "250m"
              memory: "256Mi"
